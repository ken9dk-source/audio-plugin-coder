// QuadraFuzz — UI is fully self-contained in index.html (inline JUCE interop + canvas).
// This stub exists so the binary resource set matches the other APC plugins.
import * as Juce from "./juce/index.js";
export { Juce };
